version: 2

sources:
  - name: ecomm
    database: raw
    tables:
      - name: orders_us  # This used to be orders with identifier pointing to orders_us. Now it's just plain orders_us.
        description: Each record in this table represents an order in our ecommerce store in the US
        columns:
          - name: id
            tests:
            - unique
            - not_null
      - name: orders_de
        description: Each record in this table represents an order in our ecommerce store in Germany
        columns:
          - name: id
            tests:
              - unique
              - not_null
      - name: orders_au
        description: Each record in this table represents an order in our ecommerce store in Australia
        columns:
          - name: id
            tests:
              - unique
              - not_null
      - name: orders_au_duped
        description: This table contains duplicate order records in our ecommerce store in Australia

      - name: customers
        description: Each record in this table represents a customer in our ecommerce application.
        columns:
          - name: id
            description: '{{ doc("customer_id") }}'

      - name: deliveries
        description: Each record in this table represents an order delivery in our ecommerce application.

  - name: sheets
    database: raw
    tables:
      - name: customer_survey_responses

  - name: stripe
    database: raw
    tables:
      - name: payments