version: 2

models:
  - name: fct_orders
    columns:
      - name: order_id
        tests:
          - not_null
      - name: delivery_time_from_collection
        tests:
          - greater_than_zero
      - name: delivery_time_from_order
        tests:
          - greater_than_zero
