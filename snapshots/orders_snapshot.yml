snapshots:
  - name: orders_snapshot
    relation: source('ecomm', 'orders_us')
    description: Daily snapshot of the customers table
    config:
      unique_key: id
      strategy: timestamp
      updated_at: _synced_at
      schema: snapshots