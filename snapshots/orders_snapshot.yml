snapshots:
  - name: orders_snapshot
    relation: source('ecomm', 'orders_us')
    config:
      schema: snapshots
      unique_key: id
      strategy: timestamp
      updated_at: _synced_at
