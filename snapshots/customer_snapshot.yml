snapshots:
  - name: customers_snapshot
    relation: source('ecomm', 'customers')
    config:
      schema: snapshots
      #database: analytics  # not sure yet
      unique_key: ID
      strategy: check
      check_cols:
        - FIRST_NAME
        - LAST_NAME
        - EMAIL
        - ADDRESS
        - PHONE_NUMBER